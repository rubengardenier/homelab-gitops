apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: openwebui
spec:
  ports:
    - port: 8080
      targetPort: http
      name: http
  selector:
    app: openwebui
  type: ClusterIP
