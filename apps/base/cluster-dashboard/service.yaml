apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard
  namespace: cluster-dashboard
spec:
  selector:
    app: cluster-dashboard
  ports:
    - port: 80
      targetPort: 8080
      name: http
